# Core ML packages
# RTX 4060(Ada, sm_89) 사용 시 CUDA 빌드 설치 필요:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
torch>=2.0.0
transformers>=4.30.0

# Audio processing
librosa>=0.10.0
soundfile>=0.12.1
pyaudio>=0.2.11  # 시스템에 portaudio 필요 (Ubuntu: apt install portaudio19-dev)

# Data processing
numpy>=1.21.0
scipy>=1.7.0
psutil>=5.9.0

# Configuration and utilities
pyyaml>=6.0
click>=8.0.0
tqdm>=4.64.0
colorama>=0.4.4

# Web API (FastAPI backend)
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9  # 파일 업로드(multipart) 파싱에 필수

# Optional for advanced features
datasets>=2.10.0
matplotlib>=3.5.0
seaborn>=0.11.0

# Development and testing
pytest>=7.0.0
pytest-cov>=4.0.0
black>=22.0.0
flake8>=5.0.0